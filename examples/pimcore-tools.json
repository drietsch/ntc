[
  {
    "name": "list_agent_templates",
    "description": "List agent-generated Twig templates visible to the current user.",
    "parameters": {}
  },
  {
    "name": "list_thumbnail_configs",
    "description": "",
    "parameters": {
      "type": {
        "type": "string",
        "description": "Which kind of thumbnail configs to list: \"image\" or \"video\".",
        "enum": [
          "image",
          "video"
        ],
        "required": true
      }
    }
  },
  {
    "name": "propose_agent_template_deletion",
    "description": "Propose the deletion of an existing agent-generated Twig template.",
    "parameters": {
      "slug": {
        "type": "string",
        "description": "Unique slug identifier of the template to delete.",
        "required": true
      }
    }
  },
  {
    "name": "propose_agent_template",
    "description": "Propose a new or updated Twig template for user approval.",
    "parameters": {
      "action": {
        "type": "string",
        "description": "create",
        "enum": [
          "create",
          "update"
        ],
        "required": true
      },
      "type": {
        "type": "string",
        "description": "page",
        "enum": [
          "page",
          "snippet",
          "partial"
        ],
        "required": true
      },
      "slug": {
        "type": "string",
        "description": "Unique slug identifier for the template (lowercase letters, digits, hyphens, underscores).",
        "required": true
      },
      "label": {
        "type": "string",
        "description": "Human-readable label shown in the Studio template dropdown.",
        "required": true
      },
      "main": {
        "type": "string",
        "description": "Complete Twig source for the template file.",
        "required": true
      },
      "description": {
        "type": "string",
        "description": "Optional description of the template\\s purpose."
      },
      "contextDocumentId": {
        "type": "integer",
        "description": "Optional document this proposal is about."
      },
      "reassignDocument": {
        "type": "boolean"
      }
    }
  },
  {
    "name": "get_asset_queryable_fields",
    "description": "Get all queryable fields for assets.",
    "parameters": {}
  },
  {
    "name": "get_asset",
    "description": "Get a single asset by ID with its metadata.",
    "parameters": {
      "id": {
        "type": "integer",
        "description": "The asset ID.",
        "required": true
      },
      "format": {
        "type": "string",
        "description": "Output format: \"edit\" for normalized metadata with",
        "enum": [
          "preview",
          "edit"
        ]
      },
      "includeContent": {
        "type": "boolean"
      },
      "previewBytes": {
        "type": "integer",
        "description": "Maximum bytes to include for text file preview (default 50000)."
      }
    }
  },
  {
    "name": "list_assets",
    "description": "Browse assets in a folder.",
    "parameters": {
      "parentId": {
        "type": "integer",
        "description": "Parent folder ID to list. Default: 1 (root folder)."
      },
      "page": {
        "type": "integer",
        "description": "Page number (1-based). Default: 1."
      },
      "pageSize": {
        "type": "integer"
      }
    }
  },
  {
    "name": "propose_asset_metadata_update",
    "description": "Propose custom metadata changes to one or more assets for user approval. Does NOT write — generates diffs for review. Returns {results: [...",
    "parameters": {
      "proposals": {
        "type": "array",
        "items": {
          "type": "object"
        },
        "required": true
      },
      "summary": {
        "type": "string",
        "description": "Optional one-line caption shown above the proposal review widget"
      }
    }
  },
  {
    "name": "search_assets",
    "description": "",
    "parameters": {
      "query": {
        "type": "string"
      },
      "pqlFilter": {
        "type": "string"
      },
      "parentId": {
        "type": "integer",
        "description": "Parent folder ID to scope the search. Omit for global search."
      },
      "page": {
        "type": "integer",
        "description": "Page number (1-based). Default: 1."
      },
      "pageSize": {
        "type": "integer"
      }
    }
  },
  {
    "name": "stage_asset",
    "description": "Stage a Pimcore asset to the local filesystem for analysis.",
    "parameters": {
      "id": {
        "type": "integer",
        "description": "The Pimcore asset ID to stage.",
        "required": true
      }
    }
  },
  {
    "name": "update_asset",
    "description": "Update an asset's key or custom metadata fields. Does not replace binary content. Pass field updates as an object of field/value pairs.",
    "parameters": {
      "id": {
        "type": "integer",
        "description": "The asset ID to update.",
        "required": true
      },
      "data": {
        "type": "object",
        "required": true
      }
    }
  },
  {
    "name": "upload_asset",
    "description": "Upload a new asset from base64-encoded content. Creates the asset in the specified parent folder.",
    "parameters": {
      "parentId": {
        "type": "integer",
        "description": "Parent folder ID for the new asset.",
        "required": true
      },
      "fileName": {
        "type": "string",
        "description": "Filename including extension (e.g. \"photo.jpg\", \"doc.pdf\").",
        "required": true
      },
      "base64Content": {
        "type": "string",
        "description": "Base64-encoded file content.",
        "required": true
      }
    }
  },
  {
    "name": "create_data_object",
    "description": "Create a new data object. Returns the new object ID. Use get_class_definition first to learn required fields.",
    "parameters": {
      "key": {
        "type": "string",
        "description": "Object key (filename). Must be unique within the parent folder. Use lowercase with hyphens (e.g.",
        "required": true
      },
      "classId": {
        "type": "string",
        "description": "Class ID (not name). Get this from list_classes or get_class_definition.",
        "required": true
      },
      "parentId": {
        "type": "integer",
        "description": "Parent folder ID where the object will be created. Defaults to 1 (root folder)."
      },
      "type": {
        "type": "string",
        "description": "Object type: \"object\" (default) or \"variant\".",
        "enum": [
          "object",
          "variant"
        ]
      },
      "published": {
        "type": "boolean",
        "description": "Whether to publish the object immediately. Default is false (unpublished)."
      }
    }
  },
  {
    "name": "get_class_definition",
    "description": "Get the field schema for a data object class.",
    "parameters": {
      "className": {
        "type": "string",
        "description": "Class name (e.g. \"Car\", \"Customer\"). Use list_classes to discover available class names.",
        "required": true
      },
      "justFieldNames": {
        "type": "boolean"
      }
    }
  },
  {
    "name": "get_data_object",
    "description": "Get one or more data objects by ID. Default format is \"preview\" (compact, all languages of localized fields).",
    "parameters": {
      "ids": {
        "type": "array",
        "items": {
          "type": "integer"
        },
        "required": true
      },
      "format": {
        "type": "string",
        "description": "Output format: \"preview\" (default, compact summaries) or \"edit\" (structured setter-compatible data f",
        "enum": [
          "preview",
          "edit"
        ]
      },
      "fields": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "languages": {
        "type": "array",
        "description": "Optional. Restrict every localizedFields block to just these locales (e.g. [\"en\",\"de\"]).",
        "items": {
          "type": "string"
        }
      }
    }
  },
  {
    "name": "get_field_schema",
    "description": "Get detailed schema for a specific field in a data object class. Use dot notation to drill into complex fields: localizedfields",
    "parameters": {
      "className": {
        "type": "string",
        "description": "Class name (e.g. \"Car\", \"Event\").",
        "required": true
      },
      "fieldPath": {
        "type": "string",
        "description": "Dot-separated field path (e.g. \"localizedfields\", \"attributes.Engine\", \"content.NewsCars\").",
        "required": true
      },
      "groupId": {
        "type": "integer",
        "description": "For classification store fields: get keys for this group ID."
      }
    }
  },
  {
    "name": "get_queryable_fields",
    "description": "Get all queryable fields for a data object class.",
    "parameters": {
      "className": {
        "type": "string",
        "description": "The data object class name (e.g. \"Car\", \"Customer\").",
        "required": true
      }
    }
  },
  {
    "name": "list_classes",
    "description": "List all available Pimcore data object classes.",
    "parameters": {}
  },
  {
    "name": "propose_data_object_update",
    "description": "Propose changes to one or more data objects for user approval. Does NOT write — generates diffs for review. Returns {results: [...",
    "parameters": {
      "proposals": {
        "type": "array",
        "description": "Array of proposals. Each entry: id",
        "items": {
          "type": "object"
        }
      },
      "id": {
        "type": "integer",
        "description": "Single-object shortcut: data object id. Use only when proposing"
      },
      "data": {
        "type": "object",
        "description": "Single-object shortcut: field-changes object (same shape as an entry\\s `data`). Pair with `id`."
      },
      "removeProposedFields": {
        "type": "array",
        "description": "Single-object shortcut: list of dot-paths to DROP from the proposal entirely (e.g.",
        "items": {
          "type": "string"
        }
      },
      "summary": {
        "type": "string",
        "description": "Optional one-line caption shown above the proposal review widget"
      }
    }
  },
  {
    "name": "search_data_objects",
    "description": "",
    "parameters": {
      "query": {
        "type": "string",
        "description": "Fulltext search term (searches across all indexed fields)."
      },
      "className": {
        "type": "string",
        "description": "Filter by class name (selects the class, e.g. \"<ClassName>\")."
      },
      "pqlFilter": {
        "type": "string",
        "description": "PQL filter — use this to filter by any field value (including objectType); NOT raw SQL."
      },
      "parentId": {
        "type": "integer",
        "description": "Parent folder ID to scope the search. Omit for global search."
      },
      "page": {
        "type": "integer",
        "description": "Page number (1-based). Default: 1."
      },
      "pageSize": {
        "type": "integer"
      },
      "extended": {
        "type": "boolean"
      }
    }
  },
  {
    "name": "update_data_object",
    "description": "Update field values on an existing data object. Pass a JSON object mapping field names to new values.",
    "parameters": {
      "id": {
        "type": "integer",
        "description": "The data object ID to update.",
        "required": true
      },
      "data": {
        "type": "string",
        "description": "JSON object of field values to set. nameRed Car",
        "required": true
      }
    }
  },
  {
    "name": "create_document",
    "description": "Create a new EMPTY document (Page, Snippet, Email, Link, Hardlink, or Folder). Returns the new document id.",
    "parameters": {
      "key": {
        "type": "string",
        "description": "Document key (URL slug / filename). Lowercase with hyphens (e.g. \"my-new-page\").",
        "required": true
      },
      "parentId": {
        "type": "integer",
        "description": "Parent document id (typically a folder).",
        "required": true
      },
      "type": {
        "type": "string",
        "description": "Document type.",
        "enum": [
          "page",
          "snippet",
          "email",
          "link"
        ],
        "required": true
      },
      "title": {
        "type": "string",
        "description": "Page title (used as document title for SEO and navigation; only meaningful for type=page)."
      },
      "navigationName": {
        "type": "string",
        "description": "Navigation name (only meaningful for type=page; stored as a property)."
      },
      "template": {
        "type": "string",
        "description": "Twig template (e.g. \"content/default.html.twig\"). Used when type=page/snippet/email."
      },
      "docTypeId": {
        "type": "string"
      },
      "published": {
        "type": "boolean",
        "description": "Whether to publish immediately. Default false — recommended to leave unpublished"
      }
    }
  },
  {
    "name": "get_area_brick",
    "description": "Return the full schema (editables[], branches[], usageHint, examples[]) for ONE OR MORE Pimcore areabricks in a single call.",
    "parameters": {
      "brickIds": {
        "type": "string",
        "required": true
      }
    }
  },
  {
    "name": "get_document_schema",
    "description": "",
    "parameters": {
      "id": {
        "type": "integer",
        "description": "Pimcore document ID.",
        "required": true
      }
    }
  },
  {
    "name": "get_document",
    "description": "Get one or more Pimcore documents. By default returns only the document spine",
    "parameters": {
      "ids": {
        "type": "array",
        "items": {
          "type": "integer"
        },
        "required": true
      },
      "includeContent": {
        "type": "boolean",
        "description": "Include the document content (the editable widgets and their current values, areablock entries, bloc"
      },
      "includeSettings": {
        "type": "boolean",
        "description": "Include document settings (title, description, controller, template, prettyUrl). Default false."
      },
      "includeProperties": {
        "type": "boolean",
        "description": "Include document properties. Default false. Opt in when reading or editing properties."
      },
      "targetGroupId": {
        "type": "integer"
      }
    }
  },
  {
    "name": "list_document_types",
    "description": "List the Pimcore Document Types (presets) configured for this project.",
    "parameters": {}
  },
  {
    "name": "list_editables",
    "description": "Return the sorted list of Pimcore document editable type names available in this project (e.g.",
    "parameters": {}
  },
  {
    "name": "list_target_groups",
    "description": "List active Pimcore personalization target groups (used to author target-group-specific document variants). Returns",
    "parameters": {}
  },
  {
    "name": "propose_document_update",
    "description": "Propose changes to one or more Pimcore documents for user approval.",
    "parameters": {
      "proposals": {
        "type": "array",
        "description": "id",
        "items": {
          "type": "object"
        }
      },
      "id": {
        "type": "integer",
        "description": "Single-document shortcut: document id. Use only when proposing changes for ONE"
      },
      "data": {
        "type": "object",
        "description": "Single-document shortcut: full data envelope ({editables?, settings?, properties?, appendEditables?"
      },
      "editables": {
        "type": "object",
        "description": "Single-document shortcut: editables map (alternative to `data.editables`)."
      },
      "settings": {
        "type": "object",
        "description": "Single-document shortcut: settings map (alternative to `data.settings`)."
      },
      "properties": {
        "type": "object",
        "description": "Single-document shortcut: properties map (alternative to `data.properties`)."
      },
      "targetGroupId": {
        "type": "integer",
        "description": "Single-document shortcut: personalization target-group id (from list_target_groups)."
      },
      "summary": {
        "type": "string",
        "description": "Optional one-line caption shown above the proposal review widget"
      }
    }
  },
  {
    "name": "search_documents",
    "description": "",
    "parameters": {
      "query": {
        "type": "string",
        "description": "Fulltext search term. Searches document key/title/path."
      },
      "pqlFilter": {
        "type": "string"
      },
      "parentId": {
        "type": "integer",
        "description": "Parent folder ID to scope the search. Omit for global search."
      },
      "excludeFolders": {
        "type": "boolean",
        "description": "Exclude folder documents from results. Default: false."
      },
      "page": {
        "type": "integer",
        "description": "Page number (1-based). Default: 1."
      },
      "pageSize": {
        "type": "integer"
      }
    }
  },
  {
    "name": "update_document",
    "description": "",
    "parameters": {
      "id": {
        "type": "integer",
        "description": "Document ID to update.",
        "required": true
      },
      "data": {
        "type": "object",
        "required": true
      },
      "targetGroupId": {
        "type": "integer"
      }
    }
  },
  {
    "name": "stage_file_set",
    "description": "Copy a developer-curated set of project files to this chat session's workspace so you can read them with view/grep/glob.",
    "parameters": {
      "name": {
        "type": "string",
        "description": "The file-set name to stage (e.g. \"cms_layouts\").",
        "required": true
      }
    }
  },
  {
    "name": "describe_tool",
    "description": "Get the full parameter schema for a Pimcore tool.",
    "parameters": {
      "toolName": {
        "type": "string",
        "description": "The tool name (e.g. \"search_data_objects\").",
        "required": true
      }
    }
  },
  {
    "name": "discover",
    "description": "Discover available Pimcore tool groups and tools. Call without parameters to list groups. Pass a group name to list tools in that group.",
    "parameters": {
      "group": {
        "type": "string",
        "description": "Group name to list its tools. Omit to list all available groups."
      },
      "query": {
        "type": "string",
        "description": "Keyword to search tools by name/description across all allowed groups."
      }
    }
  },
  {
    "name": "execute",
    "description": "Execute a Pimcore tool by name with an arguments object.",
    "parameters": {
      "toolName": {
        "type": "string",
        "description": "The tool name to execute (e.g. \"search_data_objects\").",
        "required": true
      },
      "arguments": {
        "type": "object"
      }
    }
  },
  {
    "name": "assign_tag",
    "description": "Assign an existing tag to an element (data object, asset, or document). Use list_tags to find tag IDs.",
    "parameters": {
      "elementType": {
        "type": "string",
        "description": "object",
        "enum": [
          "object",
          "asset",
          "document"
        ],
        "required": true
      },
      "elementId": {
        "type": "integer",
        "description": "The element ID to tag.",
        "required": true
      },
      "tagId": {
        "type": "integer",
        "description": "The tag ID to assign.",
        "required": true
      }
    }
  },
  {
    "name": "create_tag",
    "description": "Create a new tag. Returns the new tag ID.",
    "parameters": {
      "name": {
        "type": "string",
        "description": "Tag name.",
        "required": true
      },
      "parentId": {
        "type": "integer",
        "description": "Parent tag ID. Use 0 for a root-level tag."
      }
    }
  },
  {
    "name": "get_element_tags",
    "description": "Get all tags assigned to a specific element (data object, asset, or document).",
    "parameters": {
      "elementType": {
        "type": "string",
        "description": "object",
        "enum": [
          "object",
          "asset",
          "document"
        ],
        "required": true
      },
      "elementId": {
        "type": "integer",
        "description": "The element ID.",
        "required": true
      }
    }
  },
  {
    "name": "list_tags",
    "description": "List available tags. Optionally filter by parent tag to browse the tag hierarchy.",
    "parameters": {
      "parentId": {
        "type": "integer",
        "description": "Parent tag ID to list children of. Omit or set to 0 to list root-level tags."
      },
      "filter": {
        "type": "string",
        "description": "Filter tags by name (substring match)."
      },
      "page": {
        "type": "integer",
        "description": "Page number (1-based). Default: 1."
      },
      "pageSize": {
        "type": "integer",
        "description": "Results per page. Default: 50."
      },
      "timeoutMs": {
        "type": "integer",
        "description": "Soft wall-clock budget in milliseconds. Default: 5000."
      }
    }
  },
  {
    "name": "propose_tag_assignment",
    "description": "Propose tag changes for one or more elements for user approval. Does NOT write — generates diffs for review. Returns {results: [...",
    "parameters": {
      "proposals": {
        "type": "array",
        "items": {
          "type": "object"
        },
        "required": true
      },
      "summary": {
        "type": "string",
        "description": "Optional one-line caption shown above the proposal review widget"
      }
    }
  },
  {
    "name": "unassign_tag",
    "description": "Remove a tag from an element (data object, asset, or document). Use get_element_tags to see current tags.",
    "parameters": {
      "elementType": {
        "type": "string",
        "description": "object",
        "enum": [
          "object",
          "asset",
          "document"
        ],
        "required": true
      },
      "elementId": {
        "type": "integer",
        "description": "The element ID.",
        "required": true
      },
      "tagId": {
        "type": "integer",
        "description": "The tag ID to remove.",
        "required": true
      }
    }
  },
  {
    "name": "finalize_task",
    "description": "End the current task with a final result. You MUST call this exactly once to complete your task.",
    "parameters": {
      "status": {
        "type": "string",
        "description": "succeeded | failed",
        "required": true
      },
      "summary": {
        "type": "string",
        "description": "Human-readable outcome summary",
        "required": true
      },
      "output": {
        "type": "object"
      },
      "reason": {
        "type": "string",
        "description": "Reason when status=failed"
      }
    }
  },
  {
    "name": "apply_global_action",
    "description": "Apply a workflow global action to one or more Pimcore elements (max 5 per call).",
    "parameters": {
      "elements": {
        "type": "array",
        "description": "Elements to act on (1..5). id",
        "items": {
          "type": "object",
          "required": [
            "id",
            "type"
          ],
          "properties": {
            "id": {
              "type": "integer"
            },
            "type": {
              "type": "string",
              "enum": [
                "asset",
                "document",
                "data-object"
              ]
            }
          }
        },
        "required": true
      },
      "workflowName": {
        "type": "string",
        "description": "Workflow name.",
        "required": true
      },
      "globalActionName": {
        "type": "string",
        "description": "Global action name to apply.",
        "required": true
      },
      "note": {
        "type": "string",
        "description": "Optional workflow note (stored as the standard workflow comment)."
      },
      "additionalData": {
        "type": "object",
        "description": "Optional additional data forwarded as workflowOptions custom fields."
      }
    }
  },
  {
    "name": "apply_transition",
    "description": "Apply a workflow transition to one or more Pimcore elements (max 5 per call).",
    "parameters": {
      "elements": {
        "type": "array",
        "description": "Elements to transition (1..5). id",
        "items": {
          "type": "object",
          "required": [
            "id",
            "type"
          ],
          "properties": {
            "id": {
              "type": "integer"
            },
            "type": {
              "type": "string",
              "enum": [
                "asset",
                "document",
                "data-object"
              ]
            }
          }
        },
        "required": true
      },
      "workflowName": {
        "type": "string",
        "description": "Workflow name.",
        "required": true
      },
      "transitionName": {
        "type": "string",
        "description": "Transition name to apply.",
        "required": true
      },
      "note": {
        "type": "string",
        "description": "Optional workflow note (stored as the standard workflow comment)."
      },
      "additionalData": {
        "type": "object",
        "description": "Optional additional data forwarded as workflowOptions custom fields."
      }
    }
  },
  {
    "name": "get_element_workflow_details",
    "description": "",
    "parameters": {
      "elementId": {
        "type": "integer",
        "description": "Pimcore element ID.",
        "required": true
      },
      "elementType": {
        "type": "string",
        "description": "Element type: \"asset\" | \"document\" | \"data-object\".",
        "enum": [
          "asset",
          "document",
          "data-object"
        ],
        "required": true
      },
      "workflowName": {
        "type": "string",
        "description": "Optional. Filter to a single workflow by name. Empty string = return all applicable workflows."
      }
    }
  },
  {
    "name": "get_workflow_graph",
    "description": "Render a workflow diagram for a specific element as SVG. Returns the raw SVG string.",
    "parameters": {
      "elementId": {
        "type": "integer",
        "description": "Pimcore element ID.",
        "required": true
      },
      "elementType": {
        "type": "string",
        "description": "Element type: \"asset\" | \"document\" | \"data-object\".",
        "enum": [
          "asset",
          "document",
          "data-object"
        ],
        "required": true
      },
      "workflowName": {
        "type": "string",
        "description": "Workflow name.",
        "required": true
      }
    }
  },
  {
    "name": "get_workflow_places",
    "description": "",
    "parameters": {
      "workflowName": {
        "type": "string",
        "description": "Workflow name (as returned by list_workflows).",
        "required": true
      }
    }
  },
  {
    "name": "list_elements_by_workflow_state",
    "description": "List Pimcore elements filtered by workflow name and (optionally) current place. Paged; pageSize is clamped to 100.",
    "parameters": {
      "workflowName": {
        "type": "string",
        "description": "Workflow name.",
        "required": true
      },
      "elementType": {
        "type": "string",
        "description": "Element type: \"asset\" | \"document\" | \"data-object\".",
        "enum": [
          "asset",
          "document",
          "data-object"
        ],
        "required": true
      },
      "stateName": {
        "type": "string",
        "description": "Optional. Filter by current place/state name. Empty = return elements in any state."
      },
      "page": {
        "type": "integer",
        "description": "Page number (>=1)."
      },
      "pageSize": {
        "type": "integer",
        "description": "Page size (1..100). Values >100 are clamped to 100."
      }
    }
  },
  {
    "name": "list_workflows",
    "description": "List configured Pimcore workflows. With no parameters returns every workflow name.",
    "parameters": {
      "elementId": {
        "type": "integer",
        "description": "Optional Pimcore element ID. Must be given together with elementType."
      },
      "elementType": {
        "type": "string",
        "description": "Optional element type: \"asset\" | \"document\" | \"data-object\". Must be given together with elementId.",
        "enum": [
          "asset",
          "document",
          "data-object"
        ]
      }
    }
  }
]
